export function initCanvasPaint(
    context: CanvasRenderingContext2D,
    dimensions: any,
) {
    context.fillStyle = '#5555aa';
    context.fillRect(
        100,
        100,
        100,
        100,
    );
}
