package com.AirplaneCoordinates.Features.PointsInRouteOfCollision;

import com.AirplaneCoordinates.Core.Plane.Cartesian.CartesianPoint;

public class describefututeposition {
    public void execute() {
        linearPointXA = linearpoint;
        linearPointXb = linearpoint;
        linearPointya = linearpoint;
        linearPointyb = linearpoint;

        collisionInX;
        collisioninY;

        if(collisionInX.x == collisioninY.x) {
            //colisão vai acontecer nos dois eixos ao mesmo tempo, ou seja, tem uma colisão
        } else {
            // colisão não vai acontecer ao mesmo tempo, não tem uma colisão.

            //será que nesses casos, vai descrever corretamente
                - same function?
                - differeent function?
                - same position?
                - parallel lines?
                - 
        }



        final var collisionPointInTheFuture = CartesianPoint(collisionInX, collisioniny);

        
    }
}
